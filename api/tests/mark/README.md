# `tests/mark/` — les tests de non-régression du fork .mark

**Convention .mark. Ce dossier n'existe pas dans le dépôt d'origine.**

> ⛔ **La règle d'entrée du registre des forks : pas une ligne de modification sans son test.**
> Registre : `agence-mark/socle-agent-vocal`, `forks/REGISTRE.md`.

## Pourquoi ces tests existent

Git ne signale un conflit que si l'amont touche **nos lignes**. S'il **déplace ailleurs** le code
que nous modifions, tout fusionne en silence, le service continue de répondre normalement, **et
notre modification ne sert plus à rien sans que personne ne l'apprenne.**

**Aucune relecture de diff n'attrape ça. Le test est le seul mécanisme qui passe au rouge tout seul.**

## Les trois règles

| Règle | Ce qu'elle impose |
|---|---|
| **Un fichier par ligne du registre** | Le nom du fichier dit quelle garantie il protège |
| **Une seule question, oui ou non** | « Ce que ma modification garantissait est-il encore vrai ? » |
| **Il doit avoir été ROUGE** | Un test qui n'a jamais échoué sur le code non patché ne prouve rien |

## Quand ils tournent

À **chaque montée de version du fork** (étape 7 de `dograh/mise-a-jour-du-fork.md` dans le socle),
et avant tout déploiement.

```bash
cd api && python -m pytest tests/mark -q
```

## L'inventaire

| Fichier | La garantie protégée | Question du labo |
|---|---|---|
| `test_pas_dappel_vers_lediteur.py` | Aucune requête ne part vers `services.dograh.com` quand les services managés sont coupés | n° 60 |
| `test_multi_organisation.py` | Une même personne peut détenir plusieurs organisations et basculer de l'une à l'autre | n° 59 |
| `test_deepgram_en_europe_sans_entrainement.py` | L'audio de l'appelant part sur `api.eu.deepgram.com` et refuse le programme d'amélioration des modèles, **sur les trois chemins Deepgram** (STT classique, STT Flux, TTS) | n° 78 |
| `test_mistral_provider.py` | Mistral reste un fournisseur **de plein droit** (raisonnement et voix Voxtral) et non un `openai` détourné : sans sa propre classe, le filtre anti-double-exécution des appels d'outils disparaît et **un SMS partirait deux fois**. Protège aussi l'adresse `api.eu.mistral.ai` par défaut | n° 74 |
| `test_reglages_mistral_exposes.py` | Les six réglages d'échantillonnage de Mistral sont **déclarés** (donc affichés à l'écran, qui se fabrique depuis le schéma) et **transmis** jusqu'à la requête, graine comprise. Protège aussi l'inverse : sans réglage renseigné, la requête reste celle d'avant, pour Mistral comme pour les treize autres fournisseurs | n° 96, 104, 107, 108 |
| `test_graine_random_seed.py` | La graine **arrive jusqu'à Mistral au lieu de rendre l'agent muet** : elle voyage dans `extra_body`, que la bibliothèque cliente transmet, et non à la racine des paramètres où cette bibliothèque **refuse le mot-clé et rejette la requête avant l'envoi** — erreur non fatale, donc agent debout et silencieux. Protège aussi **la graine à 0** (ignorée en silence alors que l'écran l'accepte), et surtout **la configuration SANS graine, celle de tous les clients** : rien ne doit s'ajouter à sa requête, sentinelle compris | n° 112 |
| `test_configuration_estampillee_sur_lappel.py` | Chaque exécution enregistre les réglages d'échantillonnage **avec lesquels elle a été jouée**, sur les **deux** chemins : l'appel téléphonique et le banc au clavier. Sans ça, deux essais joués à deux températures sont indiscernables après coup | n° 104 |
| `test_surcharge_par_service_sur_client_v2.py` | Un agent remplace **un seul service** et hérite du reste de son client, **y compris quand le client change de modèle**. Protège aussi le marqueur sans lequel l'enregistrement de la configuration du client convertit la surcharge en copie figée, en silence | n° 59 |
| `test_reglages_transcription_exposes.py` | Les **19 réglages de la transcription** — 14 pour le connecteur Deepgram classique, 5 pour Flux — sont **déclarés** (donc affichés, l'écran se fabriquant depuis le schéma), **bornés sur la documentation de Deepgram**, et **transmis** jusqu'aux paramètres de connexion. Protège surtout l'inverse : **sans aucun réglage rempli, la requête est exactement celle d'avant le chantier**, comparée à un littéral mesuré. Protège aussi que `keyterm` **n'est pas** déclaré (il appartient au Dictionnaire de l'agent) et que la copie du schéma côté écran ne dérive pas | n° 115 |
| `test_surcharge_par_service_survit_a_lenregistrement.py` | La route qui **enregistre** une surcharge par service ne la convertit pas en copie figée au passage. ⛔ **Le fichier voisin ne protégeait pas ça** : il s'arrêtait à la fonction, et la route porte sa propre conversion — neuf tests verts sur une fonctionnalité qui ne survivait pas à son propre enregistrement | n° 59 |
| `test_reglages_pipecat_voix.py` | Les réglages de la **voix** partent par un point de collecte UNIQUE vers les **17** branches de fournisseurs, et non branche par branche : une assertion porte sur le texte source, parce que c'est la seule façon d'affirmer « aucune branche ne construit sa propre liste » plutôt que « les quatre que j'ai testées ne le font pas ». Protège le filtre de balisage, le découpage du texte, les remplacements de prononciation (⛔ **littéraux**, pas des expressions régulières), et le fait qu'**un agent sans réglage reçoit exactement la liste d'avant** | n° 116 |
| `test_estampille_voix.py` | 🆕 **L'appel dit quelle VOIX il a jouée, et à quelle adresse.** Le point d'entrée est **résolu** (Deepgram replie sur l'Europe, Mistral sur le MONDIAL) par la fonction même qui construit la requête, jamais par une seconde lecture. Appels vocaux seulement, et **dans** le bloc hors temps réel — vérifié par appartenance au bloc, pas par une indentation voisine. Registre : ligne **16** |
| `test_filtre_nom_civilite.py` | 🆕 **L'agent ne PRONONCE plus le nom ni la civilité, sans abîmer le reste.** La preuve qui compte est la **zéro perte** : les 3 889 phrases réellement dites × les 20 noms des cartes, soit plus de **60 000 comparaisons**, aucune phrase modifiée sans raison. Plus le comptage des phrases concernées, dans l'autre sens. Registre : ligne **17** |
| `test_filtre_nom_civilite_branchement.py` | 🆕 **Le filtre est branché, au bon endroit, et il tient le flux.** Le nom **coupé en deux frames** est quand même retiré ; le texte retiré **ne revient pas dans la mémoire du modèle** ; une **interruption** ne soude pas deux tours ; le drapeau « ne pas dire » survit ; la parole injectée par le moteur est filtrée aussi. Registre : ligne **17** |
| `test_lecture_epellation.py` | 🆕 **Ce que l'appelant ÉPELLE arrive dans la fiche, lettre pour lettre.** Sur les 759 runs, 25 épellations de nom ; la variable extraite n'en portait la forme épelée que **17 fois**. Corpus de **136 cas** (106 réels, 15 écrits, 15 négatifs), vérité extraite mécaniquement et jamais recopiée du lecteur : 136/136, **zéro fausse lecture**. Porte les trois règles anti-faux-positif, chacune payée par un défaut mesuré (« il n'y a » → « Nya », « d'accès… dire » → « ddd », « il y a de la fumée » → « Yad »), et **le comptage dans les deux sens** sans lequel un lecteur muet rendrait tout vert. 🔑 Une épreuve de mutation a montré que la garde d'élision écrite dans le lecteur était **du code mort** : c'est le motif de découpe des mots qui écarte les élisions, et un test dédié le protège |
| `test_recherche_voie.py` | 🆕 **La rue dite est retrouvée, ou déclarée introuvable — jamais inventée.** 🔴 La garantie qui prime : **zéro fausse sûre** sur les 438 cas des deux corpus (75 phrases réelles des runs, 63 rues inventées, 300 rues réelles dites en 8 kHz puis transcrites). 27/27 des rues réelles retrouvées, 234/300 du banc sonore. Protège aussi **les clés stockées contre celles que le code recalcule** : c'est le seul mécanisme qui voit une divergence entre le script de génération et la recherche — retirer « cour » de la liste des types le fait rougir, et c'est exactement le défaut qui avait fait annoncer « Rue d'Alger » pour « Cour d'Alger ». Même garde que `test_base_communes.py` |
| `test_adresses_epellation_branchement.py` | 🆕 **Les deux lecteurs sont BRANCHÉS, au bon endroit, dans le bon ordre.** Des lecteurs justes ne prouvent rien tant qu'on n'a pas traversé la route d'écriture (défaut du 14/09). Tient l'**ordre** (épellation avant les nombres, sinon « deux T » devient « 2 T » ; la rue après, sinon un numéro dicté en lettres n'est jamais vérifié), la **commune des tours précédents** (vos agents demandent la ville avant la rue : sans cette relecture, la rue n'était vérifiée sur aucun tour), les **deux interrupteurs** lus chacun pour soi, les **traces**, le verbatim intact, le clavier du labo. Rouges prouvés : branchement retiré → 9 rouges, relecture du registre retirée → 8 rouges |
| `test_latence_adresses_epellation.py` | 🆕 **Les deux lecteurs ne coûtent pas le tour de parole.** Plafonds larges exprès : ce qui doit rougir est un motif qui explose ou une clé recalculée à l'appel, pas une machine lente. Protège le **cache borné des communes** (sans lui chaque tour relirait le SQLite, 64 ms pour Paris) et le fait que **les clés sont stockées** (les recalculer coûtait 1,9 s pour Paris) |
| `test_reglages_pipecat_tour_de_parole.py` | Les **15 réglages du tour de parole** arrivent dans les objets que le pipeline CONSTRUIT (stratégies, détecteur, Smart Turn, agrégateur), et **un agent sans réglage construit les mêmes objets qu'avant, paramètre par paramètre**, comparés à des littéraux mesurés. 🔑 Porte **deux gardes de dérive** : nos constantes contre les défauts de la version de Pipecat embarquée (rouge le jour d'une montée de version qui déplace un défaut), et **les défauts de l'écran contre ceux du schéma Python** (l'écran en porte forcément sa propre copie) | n° 116 |
| `test_filtre_de_bruit_rnnoise.py` | Le filtre de bruit atteint les **huit** transports (le navigateur et les sept téléphonies), et l'import de `pyrnnoise` reste **paresseux** — vérifié sur l'ARBRE SYNTAXIQUE et non par un essai d'import, le paquet étant installé ici : un import au sommet passerait inaperçu et ne tomberait que sur une image construite sans l'extra, où il empêche **l'API de démarrer**, pour tous les clients | n° 77, 116 |
| `test_relance_inactivite.py` | Les consignes de relance et leur nombre sont ceux de l'agent, et **un agent sans réglage envoie les deux mêmes textes anglais, au même moment, et raccroche au même silence**. ⛔ Protège le **zéro** : lu avec `or`, un agent réglé pour raccrocher au premier silence relancerait quand même | n° 116 |
| `test_coupure_du_micro.py` | Les stratégies qui coupent le micro de l'appelant, **dans leur ORDRE** : Pipecat parcourt la liste et la première qui répond « couper » l'emporte, donc une comparaison d'ensembles dirait égales deux listes qui ne se comportent pas pareil. Protège aussi que c'est bien **la règle du workflow** qui est branchée, et non un remplaçant | n° 116 |
| `test_estampille_reglages_pipecat.py` | Chaque appel **vocal** enregistre les 27 réglages Pipecat **avec lesquels il a été joué, valeurs par défaut comprises** — sans quoi un résultat d'A/B est illisible dès qu'un patch déplace un défaut. Protège **dans les deux sens** : aucun réglage exposé n'échappe à l'estampille, et l'estampille n'emporte pas toute la configuration (surcharges, secrets). ⛔ Le banc au clavier n'estampille rien de tout cela | n° 104, 116 |
| `test_transmission_de_la_configuration.py` | 🚨 **Le pipeline DONNE-t-il la configuration de l'agent à chacun de ses points de collecte ?** Mesuré trois fois le 14/09 : retirer `run_configs` à un point d'appel laisse **tous les autres tests verts**, les points de collecte étant testés directement — ils répondent très bien sur une configuration que le pipeline ne leur donne plus, et l'agent tourne alors sur les défauts quoi qu'affiche l'écran. Assertions sur le texte source, avec **ce qu'elles ne couvrent pas écrit dedans** | n° 116 |
| `test_duplication_agent.py` | La copie d'un agent garde son `workflow_configurations` **à l'identique** — marqueur de surcharge par service et réglages Pipecat compris — et c'est une copie **profonde**. 🔑 Une branche d'A/B EST une copie : deux branches qui partagent un dictionnaire bougeraient ensemble, et le banc comparerait un agent avec lui-même sans que rien n'ait l'air anormal. Rien ne gardait ce chemin d'écriture avant le 14/09 | n° 116 |
| `test_conversion_nombres_transcription.py` | Les nombres dictés arrivent au modèle **en chiffres** par l'**étape unique de lecture de l'appelant** (`lecture_appelant.py`, plan nombres-dictes). 🔒 **Deux interrupteurs éteints : aucune étape, liste des processeurs identique** ; pour chacune des trois autres combinaisons, l'étape est **juste avant le modèle, rien avant l'agrégateur**, une seule, absente du temps réel. ✅ **Au niveau de la fonctionnalité, vrai agrégateur** : un téléphone dicté est **enregistré en mots** (N1) et **lu en chiffres** par le modèle et l'extraction ; « Saint-Maximin soixante sept cent quarante » à l'étape `coordonnees` → 60740 et mention sûre. Mention de **référence seulement à une étape qui recueille `reference…`** (N4), montant ambigu partout (N3). Trace `nombres_lus` écrite **une fois** pour un contexte renvoyé deux fois, provisoire marqué ; échec du lecteur → message intact ; autre langue → rien ; clavier : même traitement. Rouges prouvés : insertion retirée de la chaîne, référence à toute étape | n° 148, 162 |
| `test_etat_ouverture.py` | Depuis des horaires saisis au format lisible, **l'état du magasin** (`OUVERT`, `PAUSE`, `FERME`, `SUR_RENDEZ_VOUS`) **et la phrase de réouverture** sont exacts : les 17 situations de l'épreuve du 15/09 (pause déjeuner, jours fériés, congés, horaires réduits, changement d'heure), la traduction, les refus avec numéro de ligne, la priorité des exceptions, les jours fériés 2027 à 2030, et **chaque instant d'une semaine a exactement un état, compté et asserté dans les deux sens**. 🔒 **Un agent sans horaires reçoit le contexte d'avant, à l'identique** ; une valeur déjà fournie n'est jamais écrasée ; des horaires invalides arrivés jusqu'à l'appel ne lèvent rien. ⚠️ Ligne `jours fériés` absente = fériés **fermés** (le prototype les laissait ouverts). **🆕 Section 15, 18/09** : les deux phrases dites au décroché viennent des **réglages de l'organisation** — phrase reprise telle quelle, crochets gardés ou vidés selon que la réouverture est connue, champ vide = rien d'annoncé, réglages illisibles = les phrases d'avant **mot pour mot** ; et l'**état forcé** gagne sur les horaires, **se lève tout seul** passé sa date (même réglage, deux instants), tient sans date, s'applique même à un agent sans horaires, mais ne passe jamais devant une valeur déjà dans le contexte (D7). 🔴 **Section 15.4, relecture du 18/09** : ce que le forçage ANNONCE n'est pas sa date de fin mais la **première ouverture des horaires à partir de cette date** — « fermé jusqu'au 03/01 00:00 » disait « nous rouvrons dimanche 3 janvier à minuit », une heure où personne n'ouvre, un jour où ce magasin est fermé. Sans horaires, la date du forçage elle-même ; rien d'ouvert sous l'horizon, rien d'annoncé ; ne lève jamais. **Section 15.5** : le corpus de rendu est le **même fichier** que celui lu par l'écran (`donnees/annonce_rendu_cas.json`), donc le premier écart entre les deux implémentations fait rougir une des deux suites. **Section 16, décision d'Evan du 18/09** : un appel **sortant n'annonce rien** (on l'a passé nous-mêmes) — et, aussi important, **tout ce qui n'est pas explicitement `outbound` annonce comme avant**. 🔴 Un run créé sans type d'appel explicite vaut `outbound` **par défaut en base**, ce qui inclut tous les bancs au clavier : le chemin clavier ne transmet donc AUCUNE direction, et un test lit sa source pour que personne ne l'y remette | n° 151 |
| `test_horaires_ouverture_reglage.py` | La route qui **enregistre** les réglages de l'agent écrit les horaires **tels que saisis**, **refuse en 422** une saisie fautive avec son numéro de ligne (rien n'est écrit), accepte le vide, et publie le champ dans la spec. ⛔ **La grammaire est vérifiée par la ROUTE, pas par le schéma** : le schéma est aussi lu sur toute la configuration au montage de l'appel, et un refus à la lecture tuait l'appel (relecture du 15/09, prouvé rouge) ; le test appelle les deux chemins de montage sur des horaires invalides, et sur des valeurs hors bornes écrites en base (5000 caractères, un nombre, `max_call_duration=0`) : la fabrique de l'étape de lecture de l'appelant (`creer_lecture_appelant`, avant le 16/09 `creer_conversion_nombres`) ne relit que ses interrupteurs (contre-relecture, prouvé rouge). 🔑 **Et un agent sans horaires ne journalise AUCUNE erreur** : avant la déclaration du champ, le contexte était juste mais une erreur partait à chaque appel — vert pour une mauvaise raison, prouvé rouge | n° 151 |
| `test_etat_ouverture_branchement.py` | 🚨 **L'injection est-elle APPELÉE**, et au bon moment ? Voix : motif sur le source, **dans l'ordre** lecture de la configuration < injection < persistance < pre-call fetch. Clavier : **exécuté** jusqu'au moteur, base simulée, heure fixée — le contexte persisté porte les trois variables et **la consigne rendue dit « État : PAUSE »**, pas « État : . » (l'appel réel du 15/09). Le pre-call fetch gagne ; une valeur injectée au rejeu reste ; l'état du premier tour reste aux tours suivants ; sans horaires, dictionnaire persisté exact (depuis le 16/09, avec la date et l'heure de l'appel et rien d'autre). ⚠️ Ne couvre pas un moteur qui rendrait depuis un autre dictionnaire | n° 151 |
| `test_annonce_ouverture_stockage.py` | Les **phrases du décroché et l'état forcé** de l'organisation : relus à l'identique, et **refusés en 422 sans rien écrire** quand ils seraient mal entendus — crochet jamais refermé, variable qui n'existe pas (`{reouvertue}` serait dite à voix haute), date de fin sans état forcé. ⛔ **Deux règles opposées, exprès** : une ligne illisible ne coûte **rien à l'appel** (phrases par défaut, aucune exception, même base tombée) mais fait **refuser la lecture de l'ÉCRAN** en 500 — vu vide, l'enregistrement suivant remplacerait des réglages bien présents. 🔒 Les deux phrases par défaut sont celles d'avant, **asserté mot pour mot** ; la liste des états offerts à l'écran est celle de l'appel, **dans les deux sens et comptée** ; une organisation ne lit jamais les réglages d'une autre. **Relecture du 18/09** : une **accolade orpheline** est refusée (le motif exigeait une fermante pour voir un jeton, donc « Fermé { en ce moment » partait à la voix), les refus sont **en anglais** comme le reste de l'écran, et une date de fin portant un fuseau est **ramenée à l'heure de Paris** au lieu d'être tronquée (sinon l'écran la réaffichait décalée d'une heure et l'enregistrement suivant figeait le décalage) | n° 151 |
| `test_cle_de_cache_mistral.py` | 🚨 **Chaque requête de conversation vers Mistral porte la clé de cache de l'agent** (`mark-wf-<id>`), sans quoi Mistral ne sert rien depuis son cache (0 jeton mesuré le 15/09, 8 % en appel réel). ✅ **Vérifié sur le corps RÉELLEMENT ENVOYÉ** : client OpenAI du service, seule sa couche réseau remplacée, `prompt_cache_key` et `random_seed` à la racine du JSON. Graine et clé ensemble ; requête acceptée par le client. 🔒 **Sans clé, requête Mistral identique** au constructeur amont ; **OpenAI identique avec ou sans clé**. Branchement **dans les deux sens et compté** : seul l'appel de conversation reçoit la clé (voix : pas le canal realtime, ni l'extraction, ni la messagerie ; clavier : pas l'extraction). Estampille `llm_prompt_cache_key` sur un run clavier Mistral, **absente** ailleurs (dictionnaire exact). ⚠️ **Ne couvre pas, par construction** : pour Mistral, l'extraction et le résumé réutilisent le service de la conversation, leurs requêtes portent aussi la clé (sans effet sur les réponses) | n° 89 |
| `test_date_heure_appel.py` | **La date et l'heure de l'appel sont figées au décroché** (`date_appel` « mardi 15 septembre 2026 », `heure_appel` « 14 heures 32 », heure de Paris), **pour tous les agents** : une consigne qui lit l'heure à la seconde change son propre début à chaque nœud et casse le cache. Formats exacts sur minuit, « minuit 30 », midi pile, 9 h 05, 1 heure, le 1er du mois, le 31 décembre et le passage à l'an neuf, **les deux changements d'heure** ; valeur fournie conservée ; entrée non modifiée ; **ne lève jamais**. Voix : motif dans l'ordre état d'ouverture < date < persistance < pre-call fetch. Clavier : **exécuté**, la consigne rendue dit « Nous sommes le mardi 15 septembre 2026, il est 14 heures 32 » ; l'heure du premier tour reste aux tours suivants | n° 89 |
| `test_politique_derreur_pipecat.py` | 🚨 **Quelle panne de fournisseur TERMINE l'appel, et laquelle laisse l'agent debout et muet ?** Depuis Pipecat 1.8 une erreur ne porte plus son verdict : elle est classée, une catégorie **permanente** coûte au service son utilisabilité, et le pipeline applique sa politique — Dograh pose `CANCEL`, assertée sur le worker **construit** (le défaut de Pipecat est `CONTINUE`, sur lequel une clé refusée laisserait l'agent muet tout l'appel). ⛔ **La machinerie est à l'amont ; ce qui est protégé ici, c'est notre DÉPENDANCE** et surtout la **frontière** : 400, 401, 403, 404, 422 raccrochent ; 🔴 **402, 429 et les 5xx NON** — c'est exactement ce qui laisse le chantier `delai-modele` nécessaire. Frontière assertée **dans les deux sens et comptée** (une catégorie ajoutée en amont passe au rouge), code inconnu non permanent, et l'entonnoir de terminaison rend le même verdict. ⚠️ Sur la version d'avant le 16/09 ce fichier **ne s'importe même pas** : c'est un rouge pour la mauvaise raison, écrit ici plutôt que revendiqué | n° 154, n° 157 |
| `test_aucun_asyncio_run_dans_nos_tests.py` | **Aucun de nos tests n'appelle `asyncio.run()`**, qui modifie en sortant un réglage du processus hérité par tous les tests suivants. Lu dans l'arbre syntaxique de `tests/mark/`, pas par recherche de texte, et prouvé rouge sur un faux fichier. ⛔ **Ce qu'il ne prétend pas** : protéger à lui seul les tests d'amont. Le 16/09, un test d'amont tombait sur « no current event loop » ; nos `asyncio.run()` n'étaient qu'un déclencheur parmi trois, et la cause racine était le test d'amont lui-même, corrigé à la source. Né de la première CI jouée sur nos branches |
| `test_enveloppe_coupure_du_micro.py` | 🚨 **L'enveloppe de compatibilité rend-elle EXACTEMENT ce que rendait la fonction de l'amont qu'elle remplace** — mêmes stratégies, même ordre, même longueur, dans les deux états (supervision de décroché active ou non) ? ⛔ Elle n'existe que parce que **deux fichiers de tests de l'amont l'importent par son nom** : l'avoir retirée les faisait échouer à la **COLLECTE**, soit une seizaine de tests muets — et c'étaient ceux qui avaient vu un `NameError` que nos 523 tests ne voyaient pas. 🔑 **Sa fidélité repose sur deux défauts de schéma à `False`** (`mute_first_speech`, `mute_always`) et **rien ne l'assertait** ; leur test d'amont ne regarde que l'élément `[0]`, donc le jour où un réglage d'écran fait passer l'un des deux à `True`, l'enveloppe rendrait 4 ou 5 stratégies contre 3 promises **et leur test resterait vert**. L'ordre est asserté (Pipecat parcourt la liste, la première qui répond « couper » gagne), le rappel du moteur est asserté **par identité** et non par type, et les deux défauts sont épinglés | n° 116 |
| `test_base_communes.py` | La **base nationale des communes** embarquée se charge (34 000 à 36 000 communes, Saint-Maximin 60589 en 60740), **hors de la boucle d'événements et une seule fois**, et 🔴 **les clés sonores du fichier sont celles que le code calcule aujourd'hui** (200 communes tirées + les noms du banc) : changer `normaliser` ou `cle_sonore` sans régénérer le fichier ferait comparer la clé d'aujourd'hui de ce que dit l'appelant à la clé d'hier des communes, sans aucun échec visible. Codes postaux partagés triés par population. 🔴 **Les sons espeak-ng stockés (clés v2, 17/09) sont ceux que la bibliothèque rend aujourd'hui** : une nouvelle version d'espeak-ng sans régénération ferait comparer les sons d'aujourd'hui à ceux d'hier | n° 142, 164 |
| `test_analyse_communes.py` | **Le banc de l'essai du 16/09 rejoué** (56 phrases, magasin « Saint-Maximin (60) », textes seuls) : **0 commune annoncée sûre et fausse**, ≥ 48 sûres et justes, **compté** ; sans magasin, les 2 sûres fausses du prototype **nommées** ; sur 25 adresses et 64 phrases du labo, les fausses « sûres » connues **nommées une à une** (« à la campagne » → Campagne, « avant-hier » → Deuillet) — ⛔ une de plus est une régression, pas un ajout à la liste. La recherche reste **ancrée** (« avec deux personnes » ne donne rien) et **la mention au mot près** (sûre, incertaine, code postal dit conservé, orthographe de la transcription) | n° 142 |
| `test_adresse_etablissement.py` | L'**adresse de l'établissement** : les deux routes d'enregistrement (organisation, agent) l'écrivent et **refusent en 422** une commune inconnue ou qui ne porte pas ce code postal, avec le **nom officiel** stocké ; route des communes d'un code postal ; résolution **agent > organisation > rien** ; ⛔ **une adresse écrite à la main hors bornes ne fait jamais tomber l'appel** (celle de l'organisation reprend) ; injection au texte exact, valeur fournie conservée, pre-call fetch gagnant. Branchement **dans l'ordre** au téléphone, et chemin clavier **exécuté** jusqu'au moteur | n° 142 |
| `test_verification_communes_branchement.py` | 🚨 **La vérification des communes est-elle BRANCHÉE, par l'étape de lecture de l'appelant ?** Interrupteur allumé par défaut, relu seul ; mention ajoutée **seulement à une étape qui recueille une commune**, **une seule fois**, sur le dernier message de l'appelant ; conversion éteinte : le modèle **garde les mots**, la mention est là ; autre langue : la vérification du 16/09 inchangée ; **la trace de l'appel tranche le code postal du tour suivant** (« Bovet » puis « soixante mille » → Beauvais) ; analyse interrompue non marquée ; boucle non retenue. ✅ **Vrai agrégateur** : le verbatim garde ce qui a été dit, le modèle et l'extraction lisent la mention. **Clavier exécuté** jusqu'au modèle, y compris un code postal dicté (60740 en chiffres). Motifs sur le source : fabrication téléphonique avec langue, adresse et nœud courant ; clavier annoté avant l'ajout. Rouges prouvés : fabrication retirée de `run_pipeline`, appel retiré du clavier | n° 142, 162 |
| `test_variables_commune_reglage.py` | 🔑 **Les étapes vérifiées suivent le réglage `variables_commune` de l'agent**, au téléphone ET au clavier (décision d'Evan du 17/09 : un client dont la variable s'appelle `ville` ou `lieu_chantier` n'a plus besoin d'un patch). Défaut `commune, commune_*, adresse*` = **la règle d'avant** : `commune`, `commune_intervention`, `adresse_intervention` déclenchent, `ville` non ; absent, nul ou vide = défaut. Liste `ville, lieu_chantier, adresse*` : `ville` et `lieu_chantier` déclenchent, **`commune` ne déclenche plus**. Valeur illisible = défaut + avertissement, l'appel continue ; relu seul. Traversé : étape fabriquée par `creer_lecture_appelant` (celle du pipeline) et **clavier exécuté** jusqu'au modèle. Route : liste écrite telle quelle, nom invalide **refusé en 422** (rien n'est écrit), vide écrit au défaut ; champ dans la spec publiée ; estampillé sur l'appel. Rouges prouvés : branchement retiré de `lecture_appelant` (3 échecs : appel, appel, clavier), code retiré (import impossible), estampille retirée | — |
| `test_lecture_nombres.py` | 🔑 **Le lecteur des nombres dictés garde toutes les lectures** : **tous les codes postaux de France** dits des deux façons (« soixante sept cent quarante » et « soixante mille sept cent quarante », 5 728 + 6 310, comptés), **le bon code est parmi les lectures dans 100 % des cas**. Chiffre par chiffre, chiffres écrits. Les 39 phrases de `donnees/nombres_dictes.json` (fiche B du banc, Mesures, éléments 39 à 41, 15/09) classées téléphone, montant, référence, code postal, département **au texte exact**, comptées. **Invariant `text2num` dans les deux sens** : hors codes postaux, références et expressions figées, le modèle lit exactement la conversion du 15/09 ; les écarts sont nommés. Montant ambigu (N3), expressions figées (N8), mentions mot pour mot. Rouges prouvés sur six mutations | n° 149, 159, 162 |
| `test_codes_postaux_dictes.py` | 🔴 **0 commune annoncée sûre et fausse quand un code postal est dit en mots.** Le banc du 16/09 **passé par le lecteur** : aucune sûre fausse, ≥ 48 ; phrases sans commune : pas une fausse sûre de plus. **N2, un cas par branche** : commune dite, trace de l'appel, commune à confirmer puis code, département dit, lecture unique, et **« soixante deux cents » seul → à confirmer, Compiègne ou Calais** (jamais sûr sur la seule proximité). **N6** : devis, facture, commande → aucune commune, aucun code postal. **Aucun téléphone, montant, référence ni expression figée ne devient une commune** sur les 39 phrases (« zéro six » → Clairoix trouvé au lot 3). 🔴 **Un code postal ne se confirme jamais lui-même** (relecture du 16/09) : répété dans le message, redit au tour suivant, ou porté par deux communes proposées, il reste à confirmer ; incertain à côté d'une commune qui ne le porte pas, il est signalé. 🆕 **La ville du tour d'avant** (décision d'Evan, 17/09, banc run 266) ne rend un code sûr que si elle porte **la lecture la plus proche du magasin** : « Clermont-Ferrand » puis « soixante trois cents » → à confirmer, Clermont-Ferrand ou Senlis ; « Senlis » puis le même code reste sûr ; sans adresse du magasin, inchangé. 🆕 **La commune à confirmer est nommée** (décision d'Evan, 17/09, banc run 268) : « Brel » → « Demande d'abord si c'est Bresles (Oise) ; si ce n'est pas elle, propose Bornel (Oise), puis Creil (Oise) », une seule proposition comprise ; **homonymes nommés avec leur département** (« Saint-Just (Hérault) … Saint-Just (Ain) », relecture du 17/09). Rouges prouvés sur seize mutations | n° 142, 159, 162 |
| `test_balayages_nombres_dictes.py` | 🔴 **Balayages : aucune commune ni aucun code postal annoncé sûr et faux.** ⛔ Critère renforcé après la 3e relecture du 16/09 : une commune sûre est fausse si ce n'est pas LA commune dite (code INSEE), ou, sans commune dite, dès que le code en porte plusieurs (l'ancien critère « la commune porte le code » laissait passer 450 mauvaises communes du bon code). Aussi : **chaque nombre de 0 à 9 999** et les formes « X cent Y » (8 900) hors étape d'adresse restent des nombres ; numéro de maison ; phrase sans commune puis code en deux tours ; décisions 4 et 5 d'Evan en cas rapides. Tous les codes de l'Oise et de la Somme dits des deux façons **après 22 amorces réelles** (« donc », « ouais », « très bien »… 7 788 phrases, comptées) ; commune et code dans le même message (Oise, Val-d'Oise, Somme) ; **commune puis code au tour suivant par la vraie trace** (Oise, Aisne et Ardennes avec le zéro initial) ; **« c'est 1 000 » à « 9 990 »** écrits exactement comme `text2num`, sans commune sûre. Cas nommés des deux relectures du 16/09 : « donc soixante cinq cents » (Ourdon), « c'est deux mille » (Dijon), « Abbécourt » puis « deux mille trois cents » (02300), « en somme ». Garde de la branche ① testée sur une détection construite. ⚠️ Lent exprès (≈ 2 min). Rouges prouvés sur chaque règle | n° 142, 162 |
| `test_nombres_pour_la_voix.py` | 🔴 **La voix dit les nombres de la réponse en mots, l'historique du modèle garde les chiffres** (plan `voix-et-communes`, V1 à V3). Codes postaux en groupe court (« soixante, cinq cent cinquante »), cas limites de V2 un par un, **les 6 310 codes postaux de France relus en chiffres sans `num2words`** ; téléphones par paires ; montants ; références par groupes ; **les 78 phrases écrites par l'agent aux runs 264 et 265**, attendu écrit pour chacune (« la référence 60300 » jamais lue en code postal) ; balises de voix intactes. Branchement : **transformation** (pas filtre : un filtre réécrit aussi l'historique, prouvé à travers Pipecat), voix françaises seulement, agrégation par phrase, après les remplacements de l'écran ; Voxtral sans horodatage des mots (sinon l'historique recevrait les mots). Une erreur rend le texte tel quel. Rouges prouvés sur six mutations | n° 164 |
| `test_corpus_communes_zero_perte.py` | 🔴 **LE FILET : aucune commune voulue par l'appelant perdue par rapport à la production, pour CINQ magasins** (règle d'Evan, 17/09 : « tu casses l'outil à chaque chantier de réparation »). 687 phrases réelles avec leur commune (appels 230 à 273, banc du 16/09, fiche A v3, noms mal transcrits des bancs dans les tournures d'appel, phrases des trois relectures du 17/09) et 220 sans commune, textes seuls sans nom de personne ni numéro dicté (`donnees/communes_corpus_reel_2026-09-17.json`). Pour Saint-Maximin, Compiègne, Coignières, Marseille et **sans adresse de magasin**, chaque commune reste **sûre, ou proposée au même rang ou mieux**, que sur `657a365b` (rang mesuré et stocké par magasin), **comptée** ; aucune commune sûre de plus ; seuls les parasites connus proposent encore. ⛔ **À jouer en premier sur toute réparation des communes** ; ne jamais baisser un `rang_production` pour passer. 🔑 Une règle mesurée sur un seul magasin ne suffit pas : un rayon de 40 km était sans perte pour Saint-Maximin mais perdait des communes pour Compiègne (17), Coignières (10) et Marseille (7) ; décision d'Evan du 17/09 : pas de rayon (option d). Les parasites connus sont **tolérés, pas voulus** : le test refuse seulement un parasite nouveau. Prouvé : sur `657a365b`, les tests des vraies communes passent pour les cinq magasins, seul le test des parasites rougit (comportement nouveau). ⚠️ Mesuré sur le chemin français (`analyser_message`) ; le chemin non français (`annoter_texte`, `_analyser_et_mentionner`) n'est pas rejoué sur le corpus | n° 160, 169 |
| `test_pas_de_commune_sur_un_mot_qui_ressemble_mal.py` | 🔴 **Parasites retirés sans perdre une vraie commune** (décision d'Evan, 17/09, fiche D : la commune à confirmer est dite à voix haute). Retirés, au texte transcrit : « C'est la maison au bout du chemin. », « C'est le bâtiment b… », « c'est à côté de la boulangerie. ». Règles, aucune ne dépend du magasin (décision d'Evan, option d : ni seuil de ressemblance ni rayon) : article qui appartient au nom (collé ou non : « la signy » reste Lassigny) ; mots de lieu (« côté », « vers ») suivis de « de » ou non introduits = préposition, sauf nom écrit exactement (« à Vers, dans le Lot »). 🔑 « Brel » propose toujours Bresles en premier ; les phrases des trois relectures gardent leur commune (dont « Grand Villiers, au Moulin. », « Bovet. Au revoir. », « Champly, à la zone industrielle. ») ; un parasite retiré ne fait plus taire la note d'un code postal dit. ⚠️ **Retirées** : la règle « il y a » (dépendait des majuscules) ; la **règle du complément** (« passe | aux choses »), parce qu'elle effaçait de vraies communes dites seules puis suivies d'un lieu, présentes en production (« Grand Villiers, au Moulin. », « Compiagnes, au Clos des Roses. », « C'est Bovet, au hameau de Marissel. », « Bovet. Au revoir. », « Champly, à la zone industrielle. », « Bouvé à l'école », « Oui Bovet à la mairie »). **Limites notées** : « L'année dernière » propose encore Lanne (Hautes-Pyrénées) et « poêle à granulés » Grandrû, Grans, Grane : ces phrases se disent hors de la question de l'adresse, le remède est l'étape d'identité dédiée dans l'agent, pas le fork ; aussi les phrases de banc « Passe à la suite. », « Passe aux choses. », « Passe à l'élément dix / quinze / suivant. », « Passe au prochain. », et « il y a marqué » (Marques), « Révérance », « crée », « poêle à bois » (Bois)… (liste complète par magasin dans le fichier de données). ⚠️ L'absence de perte est mesurée sur le chemin français (`analyser_message`) ; le chemin non français (`annoter_texte`, `_analyser_et_mentionner`) n'est pas rejoué sur le corpus. Rouges prouvés sur la version précédente (article collé, nom exact, majuscules, communes suivies d'un lieu) | n° 160, 169 |
| `test_ville_par_le_code_et_les_sons.py` | 🔴 **La ville tranchée par le code postal, les sons et la répétition, sans fausse ville sûre** (plan `voix-et-communes`, V4, V5, V8, T8). Les noms mal transcrits des bancs (« Bouvé », « Lyon Court », « C'est un monte-à-terre »…) avec leur code : sûrs en une phrase et code puis nom ; sous les seuils 75/30 décidés par Evan : à confirmer, jamais faux ; **nom au tour d'avant le code : à confirmer** (décision d'Evan) ; nom écrit exactement hors du code (« Arcueil », « Chantilly 60230 ») jamais remplacé ; mots de conversation ; répétition qui tranche (« Lyon », « Lyon Court ») et **qui ne tranche pas** (homonymes « Saint-Just », début de nom « Pont », « non, … ») ; aucune lecture au milieu d'un nom à tirets ; sons justes sous 8 fils en parallèle ; sans la bibliothèque, la vérification continue ; **fiche A v3 jouée comme un appel : 0 fausse sûre, ≥ 28 sur 30**. Rouges prouvés par mutation | n° 164, 165 |
| `test_outil_noter.py` | 🆕 **L'outil `noter_information` (fiche au fil de l'eau, lots 1 et 3, correctifs A1, A3, A5).** Éteint par défaut, l'outil n'existe pas (D12) ; noms réservés refusés ; contrôle de citation mot par mot sur tout l'appel (D41) ; une valeur non sûre n'écrase jamais une sûre (D6) ; **une seule relance par tour** sur le vrai pipeline, dans tous les ordres (D40) ; pas de relance après une question posée en notant (A1) ; refus « non dit » avec consigne (A5) ; porte en erreur qui ferme son tour ; oui/non dicté accepté | n° 206 |
| `test_declenchement_modules.py` | 🆕 **Les modules se déclenchent d'après la fiche de l'agent, plus d'après l'étape** (lot 2, D9) ; `variables_reference` ; interrupteur éteint : déclenchement d'avant, inchangé | n° 206 |
| `test_modules_derriere_outil.py` | 🆕 **Commune, rue et épellation derrière l'outil** (lot 3, D42) : lecteur par champ, traces relues, valeur non sûre et options quand le module hésite, sans trace : écrite si dite, refusée sinon (PB5, remplace D37) ; écriture officielle rendue au modèle sans le pousser à la redire (A6), deux consignes gardées (A2) | n° 206 |
| `test_fiche_balayage.py` | 🆕 **Le balayage de fin d'appel** (lot 4, D11) : seuls les champs vides, jamais d'écrasement, extraction par étape coupée (D28) ; champ déduit qui recopie un autre refusé (A4) ; numéro en conflit marqué à vérifier, une seule fois (A8) | n° 206 |
| `test_fiche_montree.py` | 🆕 **L'état de la fiche montré au modèle** (lot 6, D14, D43), sur le vrai service Mistral du fork : ajouté à la requête seulement, avant la dernière parole, jamais à l'historique ni au prompt ; relectures hors conversation intactes ; jamais la liste de ce qui manque (A7) ; numéro corrigé non noté signalé, un seul champ téléphone (A8) ; **les sept points d'accroche existent sur le service** | n° 206 |
| `test_fiche_apres_banc.py` | 🆕 **Le patch de la fiche après le banc de sortie** (plan `patch-fiche-banc`, PB1 à PB18), **bâti sur les runs réels 838 à 852** (phrases, traces des modules, valeurs du modèle) et dans l'autre sens : recopie = même suite de mots (C1) ; déduit du balayage ancré sur un mot dit (C13) ; liste fermée `valeurs` (PB3) ; commune et rue sûres du dernier tour de l'appelant, marque du tour posée par la vraie chaîne, commune ni dite ni trouvée refusée (C2, « Liancourt », « Sans-Lisle » → Senlis, Hanvec qui ne s'impose pas) ; communes du code postal proposées (C3) ; type de voie dit, au son (C4) ; pluriel (C5) ; numéro gardé (C6) ; « il y a trois ans » accepté, cause établie sur la phrase du 847 (C7) ; « annuel » refusé (C8) ; marque par le lexique (C10) ; code postal d'une commune à code unique (C12) | n° 206 |
| `test_appels_de_fonction_voix.py` | 🆕 **La voix ne dit jamais un appel de fonction écrit** (C14, run 852, tous les agents) : phrases réelles du 852 ; parenthèse ordinaire intacte ; phrase qui n'est qu'un appel sautée par un filtre ; branché sur chaque voix construite par l'usine ; **par le vrai service de voix de Pipecat** : dit sans l'appel, historique avec | n° 206 |
| `test_dates_relatives.py` | 🆕 **Les dates relatives** (D46, A5) : « l'année dernière » → 2025 au jour de l'appel par `dateparser`, mots gardés dans `<champ>_dit` ; lecteur « date » déduit du nom ; ce que la recherche de `dateparser` prenait pour des dates (« six douze », « il a deux ans ») n'en devient jamais | n° 206 |
| `test_traversants_appel.py` | 🆕 **Les tests TRAVERSANTS** (chantier `durcissement-outils-fork`, trou T2, réflexe R1) : un appel monté par le vrai `_run_pipeline` depuis la configuration de l'agent **enregistrée en base**, jusqu'à ce que le modèle reçoit et ce que la voix prononce (seuls la transcription, le modèle et le fournisseur Cartesia sont faux ; la fabrique de voix est la vraie). Lecture des nombres (allumée, éteinte), lexique de l'organisation, nombres dits en mots et appel de fonction jamais dit, note de la fiche écrite en base et nom jamais prononcé, relance d'inactivité, coupure du micro réglée. ⛔ **Éprouvé fonctionnalité par fonctionnalité** : chacune débranchée fait rougir son test et lui seul (8 mutations, 25/09) | durcissement T2 |

## Comment se lit le rouge

**Constaté le 10/09/2026**, en retirant le patch pour vérifier que ces tests protègent vraiment
quelque chose. **Un patch perdu ne se présente pas toujours comme un échec de test.**

| Ce que la console affiche | Ce que ça veut dire |
|---|---|
| `X failed` | Le code est encore là mais ne fait plus ce qu'on garantissait |
| 🔴 `Interrupted: 1 error during collection` | **Le code a DISPARU.** Le test ne peut plus importer ce qu'il vérifie, donc pytest s'arrête **avant de jouer quoi que ce soit** |

⛔ **Le second cas est le plus dangereux, parce qu'il ressemble à une panne d'environnement.**
Retirer le patch Mistral donne exactement ça :

```
ImportError: cannot import name 'MistralLLMConfiguration' from 'api.services.configuration.registry'
Interrupted: 1 error during collection
```

🔑 **Et l'interruption masque le reste de la suite** : les autres fichiers ne sont même pas joués.
Un seul patch perdu peut donc faire croire que toute la suite est cassée. Pour voir l'état réel :

```bash
python -m pytest tests/mark -q --continue-on-collection-errors
```

**La mesure de référence, au 16/09/2026 sur `chantier/verification-communes` (vérification des
communes, avant fusion) : 615 tests sur 31 fichiers, tous verts avec les patchs (616 après la relecture).** 537 sur
`mark/deploiement` à `f62b9a58` avant ce chantier (le 527 de la montée de version, plus le garde
`asyncio.run` et un test d'amont cité). 483 sur `mark/deploiement`
(fusion `d59c3788`, état d'ouverture) avant ces deux chantiers.
**🆕 Lexique métier (17/09, branche `chantier/lexique-metier`)** : quatre fichiers neufs —
`test_lexique_stockage.py` (format, bornes, stockage, import), `test_analyse_lexique.py` (le banc de
l'essai du 16/09, les cas réels, les homonymes d'une commune), `test_lexique_ecoute_et_voix.py`
(liste envoyée à la transcription, prononciations), `test_reconnaissance_lexique_branchement.py`
(place dans la chaîne, clavier, traces, variable de l'agent) et `test_interrupteurs_sons.py` (les deux interrupteurs des sons, communes et lexique) — plus le filet
`test_corpus_communes_avec_lexique.py`, qui rejoue le corpus réel des communes **lexique allumé** :
aucune commune perdue pour les cinq magasins. **Base : 1 032 verts avant le chantier** (`f9add26e`).

⚠️ **Elle change à chaque chantier** : 47 avant l'exposition des réglages Mistral, 105 le 10/09 au
soir, **113 après la réparation de la graine**, 234 après l'exposition des réglages de la
transcription, **350 après l'exposition des réglages Pipecat** (14/09), 359 après la réparation de
l'écran, **380 avec la conversion des nombres dictés** (15/09), **483 avec l'état d'ouverture** (15/09, branche, après relecture, contre-relecture et « minuit 30 »), **516 avec la clé de cache et la date de l'appel** (16/09, branche), **527 après la montée de version amont `23d22b95`** (16/09, branche : deux fichiers neufs, 6 pour la politique d'erreur de Pipecat et 4 pour l'enveloppe de coupure du micro), **615 avec la vérification des communes** (16/09, branche : quatre fichiers neufs). C'est ce nombre-là que la procédure de montée de version prend comme base —
**une mesure de référence périmée fait passer une perte de patch pour un changement de compte.**

⚠️ **Depuis Pipecat 1.10, la suite complète (`pytest tests/mark`) se FIGE au démontage**, sans aucun
test rouge : le compte de 527 ci-dessus est mesuré **module par module** (26 fichiers, un processus
par fichier, minuterie de 120 s). 🔑 **Ce n'est pas une méthode strictement plus fine** : un module
joué seul peut aussi **fabriquer un rouge** qui n'existe pas dans la suite entière (constaté sur
`api/tests/test_camb_tts_integration.py`, un test de l'amont qui dépend d'un état posé par un autre fichier). Les deux modes se
relisent l'un avec l'autre, aucun ne fait foi seul.

**Le rouge de référence, mesuré le matin sur les 47 :** sans les patchs de `registry.py`,
`check_validity.py` et `service_factory.py` : **1 erreur de collecte (les 19 de Mistral),
7 échecs (Deepgram), 21 verts.**
