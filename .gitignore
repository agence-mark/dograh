__pycache__
.DS_Store
# Environment files. Glob rather than exact names, so variants like
# .env.prod.server or .env.local are covered too. Only the committed
# *.example templates are exempt.
.env*
!.env.example
!.env.test.example
!*.env.example

# Conductor personal/per-machine overrides (settings.toml IS committed)
.conductor/settings.local.toml

# logs and run directory on production
/logs/
/run/
infrastructure/
prd/
.vercel

.devcontainer/install.local.sh
venv/
.venv/
.playwright-mcp
coturn/
*.wav
dograh_pcm_cache/
node_modules/

# Superpowers brainstorm mockups (local only)
.superpowers/
docs/superpowers/
.gstack/

# [.mark] Où la génération de l'annuaire des rues télécharge la BAN (~1 Go de
# CSV). ⛔ Jamais dans le dépôt, qui est public, ni dans le contexte Docker.
api/assets/_ban/
